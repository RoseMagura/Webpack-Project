//import function here

describe("Filter function", () => {
  test("it should be a valid url", ()=> {
    const input = []; //input from the form on the HTML page
    const ouput = []; //check if http:// and ending normally?
    expect(filerByType(input, "link").toEqual(output));
    //actual test
  });
});
